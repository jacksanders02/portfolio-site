import CentredMain from "@/global-components/centred-main";
import React from "react";

export default function Home(): React.ReactNode {
  return <CentredMain innerComponent={<h1>Home Page!</h1>} />;
}
